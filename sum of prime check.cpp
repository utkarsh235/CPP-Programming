#include<iostream>
using namespace std;
int main ()
{
	int a,b,c,i,n
	bool flag;;
	 cout << "enter the number which you wanna check is prime or not";
	 cin >> a;
	 
	 for (x=0 ;x<=a; x++)
	 {
	 	for (y = 0 ; y <=a ; y++)
	 	{
	 	   if ()
		}
	 }
} 
int prime (int w)
{
	bool flag = true ;
	for ( z=2 ;z < w ; z++ )
	{
		if ( w % z == 0)
		{ 
		  flag = false; 
		}
	}
	if ( flag == true )
	{
		
	}
}
